function cityRecord(name, population, treasury) {
  let cityInfo = {
    name,
    population,
    treasury,
  };
  return cityInfo;
}
cityRecord(
  "Santo Domingo",

  12000,

  23500
);
